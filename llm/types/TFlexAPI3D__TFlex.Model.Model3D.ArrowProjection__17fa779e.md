# TFlex.Model.Model3D.ArrowProjection

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Класс создания проекции по стрелке

## Constructors

### `ArrowProjection(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.ArrowProjection.#ctor(TFlex.Model.Document)`

Конструктор для создания новой проекции

Parameters:
- `document`: Документ, в котором создаётся новый объект

Remarks: Проекция создаётся на активной странице

### `ArrowProjection(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.ArrowProjection.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания новой проекции

Parameters:
- `document`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся проекция

## Methods

### `ArrowProjection(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.ArrowProjection.#ctor(TFlex.Model.Document)`

Конструктор для создания новой проекции

Parameters:
- `document`: Документ, в котором создаётся новый объект

Remarks: Проекция создаётся на активной странице

### `ArrowProjection(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.ArrowProjection.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания новой проекции

Parameters:
- `document`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся проекция

## Propertys

### `Arrow`

ID: `P:TFlex.Model.Model3D.ArrowProjection.Arrow`

Стрелка взгляда

### `BaseProjection`

ID: `P:TFlex.Model.Model3D.ArrowProjection.BaseProjection`

Проекция на которой расположена стрелка взгляда
