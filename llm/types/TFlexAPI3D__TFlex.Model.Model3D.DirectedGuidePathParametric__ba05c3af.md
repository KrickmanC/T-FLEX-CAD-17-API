# TFlex.Model.Model3D.DirectedGuidePathParametric

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Класс для описания системы координат копии по путям и последовательностям поверхностей

## Remarks

Форма и положение копий в параметрическом объекте задаётся несколькими путями и последовательностями поверхностей. Один путь определяет начало локальной системы координат. Положение начала системы координат задаётся параметром (нормализованный параметр [0.0, 1.0]) или смещением от начала пути (натуральная параметризация). Параметр, как правило, задаётся функцией от переменной номера копии. Направление оси X задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке). Направление оси Y задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке). Ось Z определяется как векторное произведение осей X и Y. Если ось Z не задана, то ось Z совпадает с касательной к первому пути в точке. Ось Y в этом случае определяется как вектороное произведение осей Z и X. Пересчёт выполняется в несколько этапов: определяется локальная система координат; координаты локальной системы координат передаются в переменные, от которых может зависеть форма копируемого объекта; пересчитывается объект; к полученному результату применяется преобразование из системы координат для первой копии или ЛСК (если задана) в систему координат для текущей копии. Таким образом, первый элемент массива остаётся на месте

## Constructors

### `DirectedGuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.DirectedGuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Methods

### `DirectedGuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.DirectedGuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Propertys

### `Direction`

ID: `P:TFlex.Model.Model3D.DirectedGuidePathParametric.Direction`

Способ определения направления осей

Remarks: По умолчанию, направление оси X задаётся по минимальному расстоянию

### `XPath`

ID: `P:TFlex.Model.Model3D.DirectedGuidePathParametric.XPath`

Множество проволочных моделей, образующих составной путь для определения направления оси X

Remarks: Направление оси X задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке). Это обязательный параметр

### `XSurface`

ID: `P:TFlex.Model.Model3D.DirectedGuidePathParametric.XSurface`

Последовательность поверхностей, определяющих направление оси X

Remarks: Направление оси X задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке). Это обязательный параметр

### `YPath`

ID: `P:TFlex.Model.Model3D.DirectedGuidePathParametric.YPath`

Множество проволочных моделей, образующих составной путь для определения направления оси Y

Remarks: Направление оси Y задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке)

### `YSurface`

ID: `P:TFlex.Model.Model3D.DirectedGuidePathParametric.YSurface`

Последовательность поверхностей, определяющих направление оси Y

Remarks: Направление оси Y задаётся одним из двух способов: путём (касательная к пути в точке) или последовательностью поверхностей (нормаль к поверхности в точке)
