# TFlex.Model.Model3D.GuidePathParametric.Coords

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D.GuidePathParametric`

## Propertys

### `X`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.Coords.X`

Переменная, в которую записывается расcчитанное значение X-координаты точки или вектора

Remarks: Поскольку координата изменяется в процессе пересчета, то переменная может быть только независимой

### `Y`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.Coords.Y`

Переменная, в которую записывается расcчитанное значение Y-координаты точки или вектора

Remarks: Поскольку координата изменяется в процессе пересчета, то переменная может быть только независимой

### `Z`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.Coords.Z`

Переменная, в которую записывается расcчитанное значение Z-координаты точки или вектора

Remarks: Поскольку координата изменяется в процессе пересчета, то переменная может быть только независимой
