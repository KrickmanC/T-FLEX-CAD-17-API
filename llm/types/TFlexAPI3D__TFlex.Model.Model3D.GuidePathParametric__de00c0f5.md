# TFlex.Model.Model3D.GuidePathParametric

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Класс для описания свойств проволочной модели (пути) параметрического объекта

## Remarks

Форма и положение копий в параметрическом объекте задаётся проволочной моделью (путём). Она определяет локальную систему координат. Положение начала системы координат задаётся параметром (нормализованный параметр [ 0.0, 1.0 ]) или смещением от начала пути (натуральная параметризация). Параметр, как правило, задаётся функцией от переменной номера копии. Направление осей задаётся в производных классах. Пересчёт выполняется в несколько этапов: oпределяется локальная система координат; координаты локальной системы координат передаются в переменные, от которых может зависеть форма копируемого объекта; пересчитывается объект; к полученному результату применяется преобразование из системы координат для первой копии или ЛСК (если задана) в систему координат для текущей копии. Таким образом, первый элемент массива остаётся на месте.

## Constructors

### `GuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.GuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Methods

### `GuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.GuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Propertys

### `AxisX`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.AxisX`

Переменные, в которые записывается ориентация оси X системы координат

### `AxisY`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.AxisY`

Переменные, в которые записывается ориентация оси Y системы координат

### `AxisZ`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.AxisZ`

Переменные, в которые записывается ориентация оси Z системы координат

### `GuidePath`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.GuidePath`

Множество проволочных моделей, образующих составной путь

Remarks: Это обязательный параметр

### `OffsetFunction`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.OffsetFunction`

Функция смещения от начала пути (натуральная параметризация)

Remarks: Как правило, эта функция зависит от переменной номера копии. Положение начала системы координат задаётся параметром (нормализованный параметр [0.0, 1.0]) или смещением от начала пути (натуральная параметризация). Один из этих переменных должна быть обязательно задана

### `OffsetValue`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.OffsetValue`

Переменная, в которую записывается расcчитанное значение смещения от начала пути (натуральная параметризация)

Remarks: Поскольку смещение изменяется в процессе пересчёта, то переменная может быть только независимой. Положение начала системы координат задаётся параметром (нормализованный параметр [ 0.0, 1.0 ]) или смещением от начала пути (натуральная параметризация). Тем не меннее, значения параметра и смещения могут также использоваться в переменных, от которых зависит форма копируемого объекта

### `Origin`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.Origin`

Переменные, в которые записывается положение системы координат

### `ParamFunction`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.ParamFunction`

Функция параметра (нормализованный параметр [0.0, 1.0])

Remarks: Как правило, эта функция зависит от переменной номера копии. Положение начала системы координат задаётся параметром (нормализованный параметр [0.0, 1.0]) или смещением от начала пути (натуральная параметризация)

### `ParamValue`

ID: `P:TFlex.Model.Model3D.GuidePathParametric.ParamValue`

Переменная, в которую записывается расcчитанное значение параметра(нормализованный параметр [0.0, 1.0])

Remarks: Поскольку параметр изменяется в процессе пересчёта, то переменная может быть только независимой. Положение начала системы координат задаётся параметром (нормализованный параметр [0.0, 1.0]) или смещением от начала пути (натуральная параметризация). Тем не менее, значения параметра и смещения могут также использоваться в переменных, от которых зависит форма копируемого объекта
