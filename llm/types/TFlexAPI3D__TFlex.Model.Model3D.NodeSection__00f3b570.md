# TFlex.Model.Model3D.NodeSection

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Сечение по 2D узлам

## Constructors

### `NodeSection(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.NodeSection.#ctor(TFlex.Model.Document)`

Конструктор для создания сечения по 2D узлам

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

### `NodeSection(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.NodeSection.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания сечения по 2D узлам

Parameters:
- `Doc`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся сечение

## Methods

### `NodeSection(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.NodeSection.#ctor(TFlex.Model.Document)`

Конструктор для создания сечения по 2D узлам

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

### `NodeSection(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.NodeSection.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания сечения по 2D узлам

Parameters:
- `Doc`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся сечение

## Propertys

### `Nodes`

ID: `P:TFlex.Model.Model3D.NodeSection.Nodes`

Множество узлов. Ломанная, построенная на последовательности этих узлов образуют контур сечения
