# TFlex.Model.Model3D.OneGuidePathParametric

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Класс для описания системы координат копии по пути

## Remarks

Форма и положение копий в параметрическом объекте задаётся одним путём. Он определяет локальную систему координат. Положение начала системы координат задаётся параметром (нормализованный параметр [0.0, 1.0]) или смещением от начала пути (натуральная параметризация). Параметр, как правило, задаётся функцией от переменной номера копии. Направление оси Z совпадает с касательной к пути в точке. Направление оси X совпадает с вектором кривизны или вычисляется по закону минимального кручения. Ось Y в этом случае определяется как векторное произведение осей Z и X. Пересчёт выполняется в несколько этапов: определяется локальная система координат; координаты локальной системы координат передаются в переменные, от которых может зависеть форма копируемого объекта; пересчитывается объект; к полученному результату применяется преобразование из системы координат для первой копии или ЛСК (если задана) в систему координат для текущей копии. Таким образом, первый элемент массива остаётся на месте

## Constructors

### `OneGuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.OneGuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Methods

### `OneGuidePathParametric(TFlex.Model.Model3D.Object3D)`

ID: `M:TFlex.Model.Model3D.OneGuidePathParametric.#ctor(TFlex.Model.Model3D.Object3D)`

Конструктор для создания свойств параметрического объекта

Parameters:
- `object`: Параметрический объект, для которого задаются свойства

## Propertys

### `Direction`

ID: `P:TFlex.Model.Model3D.OneGuidePathParametric.Direction`

Способ определения направления оси X

Remarks: По умолчанию, направление оси X задаётся по вектору кривизны
