# TFlex.Model.Model3D.Path3DPath

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Путь по набору проволочных моделей

## Remarks

В настоящей версии в качестве проволочных моделей можно выбирать только 3D пути. В остальных случаях путь строится не будет

## Constructors

### `Path3DPath(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.Path3DPath.#ctor(TFlex.Model.Document)`

Конструктор для создания пути по набору проволочных моделей

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

## Methods

### `Path3DPath(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.Path3DPath.#ctor(TFlex.Model.Document)`

Конструктор для создания пути по набору проволочных моделей

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

## Propertys

### `StartPoint`

ID: `P:TFlex.Model.Model3D.Path3DPath.StartPoint`

Точка начала составного пути

Remarks: В настоящей версии в качестве точки можно выбирать только 3D узлы. В остальных случаях путь строится не будет

### `Wires`

ID: `P:TFlex.Model.Model3D.Path3DPath.Wires`

Множество проволочных моделей

Remarks: В настоящей версии в качестве проволочных моделей можно выбирать только 3D пути. В остальных случаях путь строится не будет
