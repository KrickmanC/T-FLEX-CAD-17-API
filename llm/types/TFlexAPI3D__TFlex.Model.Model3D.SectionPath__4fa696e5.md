# TFlex.Model.Model3D.SectionPath

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Путь, построенный на пересечении тела и поверхности

## Constructors

### `SectionPath(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.SectionPath.#ctor(TFlex.Model.Document)`

Конструктор для создания пути на пересечении тела и поверхности

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

## Methods

### `SectionPath(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.SectionPath.#ctor(TFlex.Model.Document)`

Конструктор для создания пути на пересечении тела и поверхности

Parameters:
- `Doc`: Документ, в котором создаётся новый объект

## Propertys

### `IntBody`

ID: `P:TFlex.Model.Model3D.SectionPath.IntBody`

Тело

Remarks: В настоящей версии в качестве тела можно выбирать только операцию. В остальных случаях путь строится не будет

### `IntPlane`

ID: `P:TFlex.Model.Model3D.SectionPath.IntPlane`

Поверхность

Remarks: В настоящей версии в качестве поверхности можно выбирать только рабочую плоскость. В остальных случаях путь строится не будет

### `IntSurface`

ID: `P:TFlex.Model.Model3D.SectionPath.IntSurface`

Поверхность

Remarks: В настоящей версии в качестве поверхности можно выбирать только рабочую плоскость. В остальных случаях путь строится не будет
