# TFlex.Model.Model3D.TangentToSurfaceWorkplane

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Рабочая плоскость, касательная поверхности

## Constructors

### `TangentToSurfaceWorkplane(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.TangentToSurfaceWorkplane.#ctor(TFlex.Model.Document)`

Конструктор для создания рабочей плоскости

Parameters:
- `document`: Документ, в котором создаётся новый объект

Remarks: Рабочая плоскость создаётся на активной странице

### `TangentToSurfaceWorkplane(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.TangentToSurfaceWorkplane.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания рабочей плоскости

Parameters:
- `document`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся рабочая плоскость

## Methods

### `TangentToSurfaceWorkplane(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.TangentToSurfaceWorkplane.#ctor(TFlex.Model.Document)`

Конструктор для создания рабочей плоскости

Parameters:
- `document`: Документ, в котором создаётся новый объект

Remarks: Рабочая плоскость создаётся на активной странице

### `TangentToSurfaceWorkplane(TFlex.Model.Document,TFlex.Model.Page)`

ID: `M:TFlex.Model.Model3D.TangentToSurfaceWorkplane.#ctor(TFlex.Model.Document,TFlex.Model.Page)`

Конструктор для создания рабочей плоскости

Parameters:
- `document`: Документ, в котором создаётся новый объект
- `page`: Страница, на которой создаётся рабочая плоскость

## Propertys

### `PointNearToSurface`

ID: `P:TFlex.Model.Model3D.TangentToSurfaceWorkplane.PointNearToSurface`

Точка, задающая положение рабочей плоскости

### `TangentSurface`

ID: `P:TFlex.Model.Model3D.TangentToSurfaceWorkplane.TangentSurface`

Поверхность, которой касается рабочая плоскость
