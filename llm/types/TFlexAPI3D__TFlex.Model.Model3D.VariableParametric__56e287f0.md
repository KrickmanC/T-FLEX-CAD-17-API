# TFlex.Model.Model3D.VariableParametric

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Класс определения свойств параметрических объектов, в котором положение копий задаётся набором явных функциональных зависимостей от номера копии

## Remarks

Положение копии задаётся локальной системой координат. Нулевая точка и направление осей такой системы координат определяются функциями. К каждой копии применяется преобразование из системы координат для первой копии или ЛСК (если задана) в систему координат для текущей копии. Таким образом, первый элемент массива остаётся на месте. Если для системы координат функциональные зависимости не заданы, то, по умолчанию, она совпадает с глобальной системой координат и трансформация не выполняется.

## Propertys

### `AxisX`

ID: `P:TFlex.Model.Model3D.VariableParametric.AxisX`

Функции, задающие ориентацию оси X системы координат копии

### `AxisY`

ID: `P:TFlex.Model.Model3D.VariableParametric.AxisY`

Функции, задающие ориентацию оси Y системы координат копии

### `AxisZ`

ID: `P:TFlex.Model.Model3D.VariableParametric.AxisZ`

Функции, задающие ориентацию оси Z системы координат копии

### `Origin`

ID: `P:TFlex.Model.Model3D.VariableParametric.Origin`

Функции, задающие положение системы координат копии
