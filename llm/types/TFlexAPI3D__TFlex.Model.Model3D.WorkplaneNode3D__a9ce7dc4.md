# TFlex.Model.Model3D.WorkplaneNode3D

Assembly: `TFlexAPI3D`
Namespace: `TFlex.Model.Model3D`

## Summary

Узел, построенный по проекциям

## Constructors

### `WorkplaneNode3D(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.WorkplaneNode3D.#ctor(TFlex.Model.Document)`

Конструктор для создания узла по проекциям

Parameters:
- `document`: Документ, в котором создаётся новый объект

## Methods

### `WorkplaneNode3D(TFlex.Model.Document)`

ID: `M:TFlex.Model.Model3D.WorkplaneNode3D.#ctor(TFlex.Model.Document)`

Конструктор для создания узла по проекциям

Parameters:
- `document`: Документ, в котором создаётся новый объект

## Propertys

### `FirstProjection`

ID: `P:TFlex.Model.Model3D.WorkplaneNode3D.FirstProjection`

Первая проекция

Remarks: Если задаётся поверхность, то 3D точка определяется только одной проекцией. В любом случае, задание второй проекции необязательно

### `SecondProjection`

ID: `P:TFlex.Model.Model3D.WorkplaneNode3D.SecondProjection`

Вторая проекция

Remarks: Если задаётся поверхность, то 3D точка определяется только одной проекцией В любом случае, задание второй проекции необязательно
