# TFlex.Model.DiagnosticsMessageId

Assembly: `TFlexAPI`
Namespace: `TFlex.Model`

## Summary

Идентификатор диагностического сообщения

## Methods

### `Clone`

ID: `M:TFlex.Model.DiagnosticsMessageId.Clone`

Создает новый объект, являющийся копией текущего экземпляра.

### `CompareTo(System.Object)`

ID: `M:TFlex.Model.DiagnosticsMessageId.CompareTo(System.Object)`

Сравнивает текущий экземпляр с другим объектом того же типа и возвращает целое число, которое показывает, расположен ли текущий экземпляр перед, после или на той же позиции в порядке сортировки, что и другой объект.

Parameters:
- `obj`: Объект для сравнения с данным экземпляром.

### `Equals(System.Object)`

ID: `M:TFlex.Model.DiagnosticsMessageId.Equals(System.Object)`

Определяет, равны ли два экземпляра объекта.

### `GetHashCode`

ID: `M:TFlex.Model.DiagnosticsMessageId.GetHashCode`

Служит хэш-функцией по умолчанию.

### `IsNullOrEmpty(TFlex.Model.DiagnosticsMessageId)`

ID: `M:TFlex.Model.DiagnosticsMessageId.IsNullOrEmpty(TFlex.Model.DiagnosticsMessageId)`

Идентификатор диагностического сообщения пустой или не задан

### `ToString`

ID: `M:TFlex.Model.DiagnosticsMessageId.ToString`

Возвращает строку, представляющую текущий объект.

## Propertys

### `IsEmpty`

ID: `P:TFlex.Model.DiagnosticsMessageId.IsEmpty`

Является ли идентификатор пустым
