# TFlex.QualityManagement.FixScriptResult

Assembly: `TFlexAPI`
Namespace: `TFlex.QualityManagement`

## Summary

Результат исправления сценария, суммирующий результаты исправления отдельных проверок

## Methods

### `GetEnumerator`

ID: `M:TFlex.QualityManagement.FixScriptResult.GetEnumerator`

Получить перечислитель отдельных результатов исправлений

### `LoadFromFile(System.String)`

ID: `M:TFlex.QualityManagement.FixScriptResult.LoadFromFile(System.String)`

Загрузить результат из файла по указанному пути. При ошибке загрузки выбрасывается исключение.

### `SaveToFile(System.String)`

ID: `M:TFlex.QualityManagement.FixScriptResult.SaveToFile(System.String)`

Сохранять результат в файл по указанному пути. При ошибке сохранения выбрасывается исключение.

### `TryLoadFromFile(System.String)`

ID: `M:TFlex.QualityManagement.FixScriptResult.TryLoadFromFile(System.String)`

Загрузить результат из файла по указанному пути. При ошибке загрузки выбрасывается исключение.

### `TrySaveToFile(System.String)`

ID: `M:TFlex.QualityManagement.FixScriptResult.TrySaveToFile(System.String)`

Сохранять результат в файл по указанному пути. При ошибке сохранения возвращается false.
